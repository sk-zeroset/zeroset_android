<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zeroset">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zeroset">

        <activity android:name=".home.ui.eventBannerDetailActivity"></activity>
        <activity android:name=".search.ui.searchBarActivity" />
        <activity android:name=".search.ui.searchActivity" />
        <activity
            android:name=".login.ui.registerActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".login.ui.loginActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".contents.ui.ContentsCommentsActivity"></activity>

        <activity android:name=".shop.ui.detailActivity" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".contents.ui.ContentsDetailActivity" />
    </application>

</manifest>